apiVersion: v1
kind: Service
metadata:
  name: {{ include "recreate_deployment.fullname" . }}
  labels:
    {{- include "recreate_deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recreate_deployment.selectorLabels" . | nindent 4 }}
